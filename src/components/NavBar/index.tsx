import {FC} from 'react'
import Logo from '../logo'
import {Container} from './styles'

const NavBar: FC = () => {
  return (
    <Container>
      <Logo />
    </Container>
  )
}

export default NavBar
